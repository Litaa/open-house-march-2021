## How to Use

### Step 1: Convert .ipynb to .Rmd file

1. Open the R project

2. In your R console, run `to_Rmd()` function from `assets/converter.R` script. You can define your prefered input/output directory but the default location is `/ipynb/` (input) and `/Rmd/` (output).
 
```
source('assets/converter.R')
to_Rmd()
```

### Step 2. Compile your pdf

1. Open `parent.Rmd`
2. Adjust YAML options & chunk titles
3. Knit it to pdf